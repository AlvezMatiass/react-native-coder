import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textItemList: {
    fontSize: 18,
    padding: 10,
    marginTop: 10,
    backgroundColor: "#587B7F",
    color: "#D9DCD6",
    borderRadius: 4,
  },
});
